<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Resumeapp" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Resumeapp" %>">
    <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header>
      <%= render 'layouts/navigation' %>
    </header>
    <main role="main">
       <%= render 'layouts/messages' %>
       <%= yield %>
    </main>
    <footer>
    
            <% if !user_signed_in?%>
              <a href="/users/sign_in" ><i class="fa fa-sign-in"></i></a>
            <% else %>
              <%= link_to destroy_user_session_path, method: "delete" do %><i class="fa fa-sign-out"></i><% end %>
            <% end %> 
            
          </ul>
    </footer>
  </body>
</html>
